Go sempre terá um pacote(package) principal(main) definido com uma função principal (func main).

a importação do "fmt" nos permite formatar e retornar números, strings etc...

para rodar o código é só rodar o comando:

go run "nome.go"
